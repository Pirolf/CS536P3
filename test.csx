//decl->structDecl
struct Universe{ int dim; int age;};
//decl->varDecl
bool isMilky;
void thisIsNonsense;
//decl->fnDecl
//fnDecl->type id formals fnBody
void collideWith(bool univ, int intensity){
int universeData;
int negIntensity;
bool collided;
struct Universe solar;

universeData = "/u1/u2/u3/un" == "/u1/u2/u3/";
negIntensity = -intensity; 
solar.dim = 3;
solar.age = 4/2+3*4-2+10*11*12+5;
solar.age.whoknows--;
solar.age++;
isMilky = true;
collided = false;

if(!collided && isMilky && dim != 3 || age > 100){
 while(age > 100 && !collided){ 
 cin>>universeData;
 cout<< age<2;
 collided = age<=3 && solar.dim >= 10;
 age = 8*(2+3);
 cout<<"data read";
 cout<<age + 1 - 1;}
}else{
 return;
}
while(age < 3000){
 collide();
 collideHard(3);
 collideHarder(true, 100, "ahhhh");
 if(collided){
  return collided;
 }else{
  if(age == 3000 * 2){

       return energyLevel() + 3 * 2;
  }
 }
return collideAtMaxIntensity(2*-negIntensity-1);
}




}

